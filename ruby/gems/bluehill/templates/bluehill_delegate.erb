package <%= @delegate_package %> 
{
    import mx.rpc.AsyncToken;
    import mx.rpc.IResponder;	
    import mx.rpc.http.mxml.HTTPService;

    import com.adobe.cairngorm.control.CairngormEvent;

    import <%= @command_package %>.*; 

    /**
     * Delegate class used to invoke the HTTPService '<%= @delegate_service_name %>'.
     *
     * Generated by Bluehill - OVERWRITE 
     */
    public class <%= @delegate_subclassname %> extends <%= @delegates_superclass %> {

        /**
         * Constructor method.
         */
        public function <%= @delegate_subclassname %>(cmd : <%= @commands_superclass %>, cgmEvt : CairngormEvent) {
     
            super(cmd, cgmEvt);
        }

        public function execute() : void {

            trace('<%= @delegate_subclassname %> execute');  

            var service : HTTPService = lookupHttpService('<%= @delegate_service_name %>');

            // TODO - If necessary for your application, cast the CairngormEvent to your specific 
            // application event type and pass the appropriate parameters to the service object.

            var token : AsyncToken = service.send(cgmEvent.data);
            token.addResponder(command);
        }
    }
}
