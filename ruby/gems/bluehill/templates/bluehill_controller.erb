package <%= @control_package %> 
{
    import com.adobe.cairngorm.control.FrontController;
    import <%= @command_package %>.*
    import <%= @event_package %>.<%= @event_names_class %>; 
    
    /**
     * This class is the 'C' in the MVC design pattern.  It extends the Cairngorm FrontController,
     * and adds your CairngormEvent-to-ICommand mappings.
     *
     * Generated by Bluehill - OVERWRITE
     */
    public class <%= @controller_classname %> extends FrontController {
    
        /**
         * Constructor method.
         */
        public function <%= @controller_classname %>() { 
    
            super();
            initializeCommands();
        }
        
        public function initializeCommands() : void {
    
            trace('<%= @controller_classname %> initializeCommands starting.');

            // Associate the Cairngorm event names to their Command class implementations:
<% @command_mappings.each do | line | %>
            <%= line %>
<% end %>

            trace('<%= @controller_classname %> initializeCommands completed.'); 
        }
    }
}
