package <%= @event_package %> 
{
    import flash.events.Event;
    import com.adobe.cairngorm.control.CairngormEvent;
    import <%= @event_package %>.<%= @event_names_class %>;

    /**
     * Event class for the event named '<%= @event_name %>'.
     *
     * Generated by Bluehill - OVERWRITE
     */
    public class <%= @event_subclassname %> extends CairngormEvent {
    
        // Instance variables:
        public var parameters : Object = null;

        /**
         * Constructor method.
         */
        public function <%= @event_subclassname %>(params:Object=null) {
    
            super(<%= @event_names_class %>.<%= @event_name %>);
            parameters = params;
            data = params;
        }

        /**
         * Override the inherited clone() method, but don't return any state.
         */
        override public function clone() : Event {
    
            return new <%= @event_subclassname %>();
        }
    }
}
