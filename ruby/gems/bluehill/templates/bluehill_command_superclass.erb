package <%= @command_package %> 
{    
    import mx.rpc.IResponder;	
    import mx.rpc.events.ResultEvent;
    import mx.rpc.events.FaultEvent;
    import mx.rpc.http.mxml.HTTPService;
     
    import com.adobe.cairngorm.business.ServiceLocator;
    import com.adobe.cairngorm.control.CairngormEvent;

    import <%= @event_package %>.*;
    import <%= @command_package %>.*; 
    import <%= @model_package %>.<%= @model_locator_classname %>;
    
    /**
     * This is the superclass of the Command classes in the application.
     *
     * Generated by Bluehill - OVERWRITE
     */
    public class <%= @commands_superclass %> implements IResponder { 
    
        // Common instance variables:

        /**
         * Constructor method.
         */
        public function <%= @commands_superclass %>() {
     
            super();
        }
		   
        protected function lookupHttpService(svcName:String) : HTTPService {
          
            return ServiceLocator.getInstance().getHTTPService(svcName) as HTTPService;
        }

        protected function commonFaultHandler(event:Object, cairngormEvent:CairngormEvent) : void {
    
            // TODO: Implement your own logic as necessary.
            trace('commonFaultHandler ' + event.toString() + ' ' + cairngormEvent.toString());
        }

        public function result(event:Object) : void {
 
            trace('<%= @commands_superclass %>.result - subclasses should override.');
        }

        public function fault(event:Object) : void {

            trace('<%= @commands_superclass %>.fault - subclasses should override.');
        }
       
    }
}
