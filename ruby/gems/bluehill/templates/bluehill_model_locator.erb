package <%= @model_package %> 
{
    import com.adobe.cairngorm.model.IModelLocator;

    import mx.collections.ArrayCollection;
    import mx.collections.ICollectionView;
    
    /**
     * This is the ModelLocator singleton class for your application.  It contains instances
     * and collections of your Model objects.
     *
     * Generated by Bluehill - OVERWRITE
     */
    [Bindable] public class <%= @model_locator_classname %> implements IModelLocator {
    
        // Class variables:
        private static var modelLocator : <%= @model_locator_classname %>;

        // Instance variables:
        // TODO - add your application-specific variables and business methods.

        /**
         * Constructor method.
         */
        public function <%= @model_locator_classname %>() {
    
            if (modelLocator != null) {
                throw new Error("Only one <%= @model_locator_classname %> may be instantiated."); 
            }
            else {
              trace('<%= @model_locator_classname %> initialized.');
            }
        }

        public static function getInstance() : <%= @model_locator_classname %> {
    
            if (modelLocator == null) {
                modelLocator = new <%= @model_locator_classname %>();
            }
            return modelLocator;
        }
    }
}
