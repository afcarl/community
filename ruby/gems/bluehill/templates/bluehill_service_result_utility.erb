package <%= @util_package %> 
{
    import mx.rpc.events.ResultEvent;
    import mx.rpc.events.FaultEvent;

    /**
     * Application-specific class for optionally and additionally handling service responses,
     * such as status bar updates or response capturing and logging.
     *
     * Generated by Bluehill - OVERWRITE
     */
    public class ServiceResultUtil {
    
        public static function result(event:ResultEvent, context:String) {
    
            StatusBarUtility.update("Result: " + context + " " + event.toString()); 
            trace("Result: " + context + " " + event.toString());   
        }
        
        public static function fault(event:FaultEvent, context:String) {
    
            StatusBarUtility.update("Fault: " + context + " " + event.toString()); 
            trace("Fault: " + context + " " + event.toString());     
        }        
    }
}
